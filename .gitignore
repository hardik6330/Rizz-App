
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Secrets — these hold live keys. Never commit them.
.env
.mcp.json

node_modules/
.expo/
dist/
web-build/
*.log
.DS_Store

# Native projects are regenerated from app.json on every build (CNG) — see
# .easignore. Committing the prebuild output ships stale native code.
/android
/ios

# Firebase config — per-project, contains project API keys. Supplied to EAS as
# a file-type secret (GOOGLE_SERVICES_JSON / GOOGLE_SERVICES_PLIST), never committed.
google-services.json
GoogleService-Info.plist
.vercel
.env*

# Claude Code — local agent config and session state. The shared rulebook is
# AGENTS.md, which every agent reads and which stays tracked.
.claude/
.mcp.json
CLAUDE.md

# Local native module build output. Gradle intermediates — merged manifests,
# blame reports, jars — regenerated on every build. 240 of these were tracked,
# which is 3.4MB of noise in every clone and diff.
modules/**/build/

# macOS archive-extraction junk. Never belonged here.
__MACOSX/
